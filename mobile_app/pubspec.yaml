name: candidate_app
description: A production-ready candidate mobile application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  go_router: ^13.0.0
  flutter_secure_storage: ^9.0.0
  dio: ^5.4.0
  camera: ^0.10.5+9
  video_player: ^2.8.2
  path_provider: ^2.1.2
  permission_handler: ^11.1.0
  google_fonts: ^6.1.0
  intl: ^0.19.0
  json_annotation: ^4.8.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.8
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
